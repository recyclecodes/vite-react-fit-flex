import Navbar from '@/components/NavBar';
import Footer from '@/components/Footer';

const Layout = () => {
  return (
    <div>
      <Navbar />
      <Footer/>
    </div>
  );
};

export default Layout;
